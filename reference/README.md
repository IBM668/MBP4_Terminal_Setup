# Reference

存放本仓库开发过程中参考的原始项目快照,不参与安装流程,仅供以后查阅对照。

- `terminal-setup-main.zip` — 原始参考项目 lewislulu/terminal-setup（`main` 分支快照），本仓库 `MBP4_Terminal_Setup` 是基于其思路精简改造而来（仅保留 macOS + Zsh + Starship + 语法高亮部分）。

